Build instructions
------------------

Run the build script (PowerShell on Windows):

```powershell
./build.ps1
```

Outputs:
- `dist/` — production-ready copy of the site
- `Quiz_preFinal.zip` — zip archive of the project

Notes:
- This script performs a simple copy of files (no minification).
- Ensure PowerShell execution policy allows running local scripts.
